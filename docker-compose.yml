services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: canopy
      POSTGRES_PASSWORD: canopy
      POSTGRES_DB: canopy
    ports:
      - "5433:5432"
    volumes:
      - canopy_pgdata:/var/lib/postgresql/data

volumes:
  canopy_pgdata:
